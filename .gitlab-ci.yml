image: cypress/included:13.13.0

stages:
  - test

before_script:
  - echo "Running before_script"

test:e2e:
  stage: test
  script:
    - npm ci
    - npx cypress verify 
    - npx cypress run
  artifacts:
    when: on_failure
    paths:
      - cypress/videos
      - cypress/screenshots
